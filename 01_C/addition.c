#include <cs50.h>
#include <stdio.h>

int main(void) {
  //int x = get_int("x: ");
  //int y = get_int("y: ");
  long x = get_long("x: ");
  long y = get_long("y: ");
  //printf("%i + %i = %i\n", x, y, x + y);
  printf("%li + %li = %li\n", x, y, x + y);
}
